# Android V2 discovery verification

Validates mDNS and HTTP LAN discovery, native CameraX + ML Kit QR scanning, server payload parsing, permissions, unit tests, Android Lint, and debug APK assembly.
