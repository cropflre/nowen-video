# Offline download verification

Validates WorkManager foreground downloads, HTTP Range resume, per-account task isolation, storage quotas, pause/resume/delete controls, startup recovery, Media3 local playback, unit tests, Lint, and debug APK assembly.
